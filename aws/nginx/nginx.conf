events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    
    # 로그 형식
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log;
    
    # 기본 설정
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    
    # Gzip 압축
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;
    
    # 업로드 크기 제한
    client_max_body_size 100M;
    
    # 백엔드 업스트림
    upstream backend {
        server backend:8080;
    }
    
    # HTTP 서버: HTTPS로 리다이렉트
    server {
        listen 80;
        server_name twiiiins.com www.twiiiins.com;

        # Let's Encrypt 인증서 확인 (Certbot)
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # 모든 HTTP 요청을 HTTPS로 리다이렉트
        location / {
            return 301 https://$server_name$request_uri;
        }
    }

    # HTTPS 서버 (인증서 발급 전 주석 처리)
    # server {
    #     listen 443 ssl http2;
    #     server_name twiiiins.com www.twiiiins.com;
    #     root /usr/share/nginx/html;
    #     index index.html;
    #
    #     # SSL 인증서 설정
    #     ssl_certificate /etc/letsencrypt/live/twiiiins.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/twiiiins.com/privkey.pem;
    #     
    #     # SSL 보안 설정
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
    #     ssl_prefer_server_ciphers off;
    #     ssl_session_cache shared:SSL:10m;
    #     ssl_session_timeout 10m;
    #
    #     # HSTS (선택사항)
    #     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    #
    #     # API 프록시
    #     location /api/ {
    #         proxy_pass http://backend;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_set_header X-Forwarded-Host $server_name;
    #     }
    #
    #     # 업로드 파일은 백엔드로 프록시
    #     location /uploads/ {
    #         proxy_pass http://backend/uploads/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_set_header X-Forwarded-Host $server_name;
    #         expires 1y;
    #         add_header Cache-Control "public, immutable";
    #     }
    #
    #     # 헬스체크 (백엔드)
    #     location /health {
    #         proxy_pass http://backend/actuator/health;
    #         access_log off;
    #     }
    #
    #     # SPA 라우팅: 없는 경로는 index.html로 fallback
    #     location / {
    #         try_files $uri $uri/ /index.html;
    #     }
    # }
}
